name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: Akki-8989/Infrastructure/.github/workflows/deploy-backend.yml@main
    with:
      repo_name: be-node-orders
      infra_repo: Infrastructure
      database_type: sqlserver
      database_name: ecom_db
    secrets: inherit
